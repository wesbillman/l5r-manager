<h2 class="title">New Card</h2>
<% form_for(@card, :html => { :multipart => true, :class => "form" }) do |f| %>
	<%= f.error_messages %>
	<%= render :partial => 'form', :locals => { :f => f } %>	
  <%= f.submit "Create", :class => "button" %> or <%= link_to 'Cancel', cards_path %>
<% end %>


