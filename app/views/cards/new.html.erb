<h1>New card</h1>

<% form_for(@card, :html => { :multipart => true }) do |f| %>
	<%= f.error_messages %>
	<%= render :partial => 'form', :locals => { :f => f } %>	
  <%= f.submit "Create" %>
<% end %>

<%= link_to 'Back', cards_path %>
