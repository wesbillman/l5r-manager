<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en">
  <head>
    <title>L5R Card Manager</title>
    <%= stylesheet_link_tag('l5r', 'application', :media => "all")  %>
    <%= javascript_include_tag :defaults %>
  </head>
  <body>
  <div id="page">
    <div id="header">
			<div id="site-title">
			<%= link_to 'L5R Card Manager', root_url %>
			</div>
			<div id="site-subtitle">For managing all your L5R cards</div>
			<div id="nav">
				<ul>
				  <li <%= selected_navigation?(:card) %>><%= link_to('Cards', :controller => 'cards') %></li>
				  <li <%= selected_navigation?(:deck) %>><%= link_to('Decks', :controller => 'decks') %></li>
					<% if authorized? -%>
				  <li <%= selected_navigation?(:settings) %>><%= link_to('Settings', :controller => 'settings') %></li>
				  <% end -%>
				</ul>
			</div>
    </div>
    <div id="main">      
      <%= "<div id=\"notice\">#{flash[:notice]}</div>" if flash[:notice] %>
			<%= "<div id=\"error\">#{flash[:error]}</div>" if flash[:error] %>
      <%= yield %>
    </div>
    <div id="footer">
			<p id="site-links">
			<% if logged_in? -%>
				<%= link_to h(current_user.login), current_user %> | 
				<%= link_to 'logout', logout_url %>
      <% else -%>
        <%= link_to 'login', login_url %> or 
				<%= link_to 'register', signup_url %>
			<% end -%>
			</p>
    </div>
  </div>
  </body>
</html>