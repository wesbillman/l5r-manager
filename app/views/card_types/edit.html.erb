<h1>Editing card_type</h1>

<% form_for(@card_type) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @card_type %> |
<%= link_to 'Back', card_types_path %>
